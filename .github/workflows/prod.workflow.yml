# name: Prod Workflow - Configure, Install, Build and Deploy

# on:
#   pull_request_review:
#     types: [submitted]
# jobs:
# build:
# if: github.event.review.state == 'approved'
# runs-on: ubuntu-latest
# steps: